# ACCEPTANCE｜auth-permission

## 快照验收（已执行）

- [x] `backend/project-scaffold/` 包含认证、JWT、Token、用户相关代码。
- [x] `backend/zhongsheng-rbac/` 包含角色、菜单、权限常量和方法级鉴权参考。
- [x] `frontend/qmy-admin/` 包含登录、auth API、路由守卫、权限 store、角色菜单页面。
- [x] `db/` 包含认证表和 RBAC 表。
- [x] 不包含 `.git/`、`.DS_Store`、`target/`、`node_modules/`、`build/`、`dist/`。
- [x] 不包含真实数据库密码、JWT secret、调试 token。
- [x] `AuthSettings.jwt.secret` 无代码默认值，未配置 `auth.jwt.secret` 时启动失败。
- [x] 扫码登录通道边界已标注：飞书/钉钉为可选配置通道，企业微信为待接入占位。

## 接入验收（装配到真实项目后执行）

- [ ] 密码登录成功返回 token。范围：目标项目运行时验收。
- [ ] 飞书/钉钉扫码在平台配置完整时可登录。范围：目标项目运行时验收。
- [ ] 企业微信扫码启用前先补实现。范围：目标项目开发任务。
- [ ] 无 token 访问受保护接口返回未授权。范围：目标项目运行时验收。
- [ ] 退出登录后旧 token 失效。范围：目标项目运行时验收。
- [ ] `/sysUser/info` 返回当前用户和 `permission.curPermissions`。范围：目标项目运行时验收。
- [ ] 普通用户只能看到有权限的路由。范围：目标项目运行时验收。
- [ ] `v-permission` 能隐藏无权限按钮。范围：目标项目运行时验收。
- [ ] 后端接口缺权限时拒绝访问，不只依赖前端隐藏。范围：目标项目运行时验收。
- [ ] 角色能关联菜单权限。范围：目标项目运行时验收。
- [ ] 菜单权限标识不能重复。范围：目标项目运行时验收。

## 安全验收（已执行）

- [x] JWT secret 由环境配置或密钥系统注入，组件无代码默认值。
- [x] 不存在硬编码超级 token。
- [x] 初始化管理员密码不写死在源码中。
- [x] 第三方扫码 app secret 不进入代码仓库。
- [ ] Swagger 是否匿名开放已按目标环境确认。范围：目标项目运行时验收。

## 当前快照验证说明

- 已执行文件存在性、污染文件、环境配置、敏感字符串和 Git diff 检查。
- 未对抽取快照做完整工程编译，因为它是多来源拼装证据包，不是单一工程。
- 密码登录、token 失效、接口权限、浏览器扫码登录必须在真实项目装配后验证。
