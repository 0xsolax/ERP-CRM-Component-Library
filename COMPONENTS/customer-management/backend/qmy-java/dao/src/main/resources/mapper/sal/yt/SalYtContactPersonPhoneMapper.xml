<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiaomoyun.mapper.sal.yt.SalYtContactPersonPhoneMapper">
    
    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.qiaomoyun.entity.sal.yt.SalYtContactPersonPhone">
        <id column="id" property="id"/>
        <result column="contact_id" property="contactId"/>
        <result column="area_code" property="areaCode"/>
        <result column="phone" property="phone"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, contact_id, area_code, phone, create_user, create_time, update_user, update_time, tenant_id, is_deleted
    </sql>
    
    <!-- 根据联系人ID查询电话号码列表 -->
    <select id="selectByContactId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sal_yt_contact_person_phone
        WHERE contact_id = #{contactId}
        AND is_deleted = 0
        ORDER BY id ASC
    </select>
    
    <!-- 根据联系人ID删除电话号码（逻辑删除） -->
    <update id="deleteByContactId">
        UPDATE sal_yt_contact_person_phone
        SET is_deleted = 1
        WHERE contact_id = #{contactId}
        AND is_deleted = 0
    </update>
    
    <!-- 批量插入联系人电话号码 -->
    <insert id="batchInsert">
        INSERT INTO sal_yt_contact_person_phone
        (contact_id, area_code, phone, create_user, create_time, update_user, update_time, tenant_id, is_deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.contactId}, #{item.areaCode}, #{item.phone}, #{item.createUser}, #{item.createTime}, 
             #{item.updateUser}, #{item.updateTime}, #{item.tenantId}, #{item.isDeleted})
        </foreach>
    </insert>

    <!-- 根据电话号码查询是否存在（排除指定的联系人ID） -->
    <select id="countByPhoneExcludeContactId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM sal_yt_contact_person_phone
        WHERE phone = #{phone}
        AND is_deleted = 0
        <if test="excludeContactId != null">
            AND contact_id != #{excludeContactId}
        </if>
    </select>
</mapper>