# ACCEPTANCE｜purchase-supplier

## 快照完整性

- [x] `component.yaml` 已填写真实组件名、状态、依赖和入口。
- [x] `component.yaml` 按 SOL-50 要求标记 `status: draft`。
- [x] `README.md` 能说明组件用途、接入步骤和限制。
- [x] `SOURCE_MAP.md` 保留所有关键 `RAW/...` 来源路径。
- [x] `docs/source/` 已带出 PRD 和调研材料。
- [x] `docs/spec/` 已整理组件实现规范。
- [x] `docs/contracts/` 已覆盖 API、数据、权限。
- [x] `docs/acceptance/REVIEW_EVIDENCE.md` 已记录执行与验证证据。

## 代码与数据

- [x] `backend/` 包含 legacy 供应商 CRUD 后端快照。
- [x] `backend/` 包含 qmy-java 供应商、待采购、采购单、库存预警后端参考证据。
- [x] `frontend/` 包含供应商、待采购、已采购、库存预警页面/API/路由。
- [x] 前端产品/客户/组织直接依赖 API 已裁剪到采购页面实际调用函数，未把完整产品/客户/组织维护能力误收进组件。
- [x] `db/` 包含 legacy `supplier` SQL 和 qmy-java 复杂采购表结构说明。
- [x] 采购申请和采购单后端缺口已明确列出：完整 DDL、权限闭环、状态机、运行验收仍待补齐。

## 业务验收

- [x] 供应商 CRUD 可追溯。
- [x] 供应商联系人、标签、跟进和规格对照能力已标注为复杂流参考。
- [x] 待采购、采购申请、采购单、采购退货、库存预警页面和接口已整理。
- [x] 报价阶段不强绑定供应商、采购阶段按供应商聚合采购单的规则已写入文档。
- [x] 仓储入库、库存流水、财务付款、供应商对账边界已明确。
- [x] 未补齐正式采购模型前不得标记为可直接复用。

## 污染检查

- [x] 不包含 `.git/`。
- [x] 不包含 `.DS_Store`。
- [x] 不包含 `target/`。
- [x] 不包含 `node_modules/`。
- [x] 不包含 `dist/`、`build/`。
- [x] 不包含 `.env` 或本地运行配置。
- [x] 不包含数据库密码、OSS AccessKey、私钥等敏感信息。

## 装配验收

- [x] 能说明依赖哪些基础组件。
- [x] 能说明接入顺序。
- [x] 能说明最小可运行路径。
- [x] 能说明主要异常和权限边界。
- [x] 能说明不能直接复用的风险。

## 接入前必须补齐

- [ ] qmy-java 复杂采购正式 DDL、索引、唯一键和软删除字段。
- [ ] 供应商子资源、采购申请、采购单详情/导出/退货/通知的数据范围守卫。
- [ ] `follow/delete`、`contact/delete` 等破坏性 GET 接口的方法收口。
- [ ] 采购申请生成采购单的幂等、防重复和撤回规则。
- [ ] 采购单与仓储入库、财务付款、订单状态的联动验收。
