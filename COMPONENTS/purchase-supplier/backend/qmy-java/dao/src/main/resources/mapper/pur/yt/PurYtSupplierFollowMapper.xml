<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qiaomoyun.mapper.pur.yt.PurYtSupplierFollowMapper">

    <resultMap id="BaseResultMap" type="com.qiaomoyun.entity.pur.yt.PurYtSupplierFollow">
        <id column="id" property="id"/>
        <result column="supplier_id" property="supplierId"/>
        <result column="theme" property="theme"/>
        <result column="contact_person" property="contactPerson"/>
        <result column="description" property="description"/>
        <result column="next_visit_date" property="nextVisitDate"/>
        <result column="create_user" property="createUser"/>
        <result column="create_time" property="createTime"/>
        <result column="update_user" property="updateUser"/>
        <result column="update_time" property="updateTime"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, supplier_id, theme, contact_person, description, next_visit_date,
        create_user, create_time, update_user, update_time, tenant_id, is_deleted
    </sql>

    <!-- 根据供应商ID查询跟进记录 -->
    <select id="selectBySupplierId" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM pur_yt_supplier_follow
        WHERE supplier_id = #{supplierId}
        AND is_deleted = 0
        ORDER BY create_time DESC
    </select>

    <!-- 逻辑删除跟进记录 -->
    <update id="deleteById">
        UPDATE pur_yt_supplier_follow
        SET is_deleted = 1
        WHERE id = #{id}
        AND is_deleted = 0
    </update>
</mapper>