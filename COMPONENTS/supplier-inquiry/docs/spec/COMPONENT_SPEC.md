# 供应商询价台账组件规范

## 业务目标

沉淀采购前询价历史，让采购人员能按供应商、对象类型、对象名称、币种和日期追踪历史报价。

## 关键规则

- 保存询价不会回写主档价格。
- 主档对象只带入名称、编号、规格、单位和参考价格快照。
- 手工对象只存在于询价记录，不自动创建材料或产品。
- 编辑保存生成字段级修改日志。
- 供应商列表中的历史入口只读查看，不编辑询价记录。

## 依赖

- 供应商主档用于供应商选择和快捷新增。
- 产品物料用于询价对象搜索。
- 权限基座用于菜单、按钮和接口拦截。
