# Review Evidence

## 已确认

- `GUIDE_供应商询价台账.md` 覆盖业务边界、接口、数据、权限和验证要点。
- 后端存在 `SupplierInquiryController`、服务层、管理层、实体、VO 和 DTO。
- SQL 和启动兜底初始化器均覆盖 `supplier_inquiry`。
- 前端存在独立台账页面、API、采购管理路由和供应商/对象历史交互。

## 仍需目标项目验证

- 目标项目中的供应商主档权限和快捷新增是否一致。
- 主档对象搜索范围是否覆盖目标客户需要的物料类型。
- 历史报价是否要进入正式比价单或采购采用流程。
